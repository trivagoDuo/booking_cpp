server {
	listen *:8080 default_server;
	listen [::]:8080;

	server_name _;

	# logging
	access_log /var/log/nginx/000-default.access.log;
	error_log /var/log/nginx/000-default.error.log warn;

	include nginxconfig.io/general.conf;
}